<!-- <!DOCTYPE html>
<html lang="janken2">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>あっち向いてホイ</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      text-align: center;
      padding: 20px;
    }
    .result {
      margin-top: 20px;
    }
    .game-controls {
      margin: 20px 0;
    }
    .buttons {
      margin: 10px 0;
    }
  </style>
</head>
<body>
  <h1>あっち向いてホイ - ゲーム</h1>

  <div>
    <p>「上」「右」「下」「左」のいずれかを選んでください：</p>
    <form action="/janken2" method="get" class="game-controls">
      <input type="radio" name="direction" value="上" id="up" required>
      <label for="up">上</label>
      <input type="radio" name="direction" value="右" id="right">
      <label for="right">右</label>
      <input type="radio" name="direction" value="下" id="down">
      <label for="down">下</label>
      <input type="radio" name="direction" value="左" id="left">
      <label for="left">左</label>
      <br>
      <button type="submit">選択</button>
    </form>
  </div>

  <% if (userDirection && cpuDirection) { %>
    <div class="result">
      <p><strong>あなたの選んだ方向:</strong> <%= userDirection %></p>
      <p><strong>コンピューターの選んだ方向:</strong> <%= cpuDirection %></p>
      <p><strong>結果:</strong> <%= result %></p>
    </div>
  <% } %>

</body>
</html> -->

<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <title>あっち向いてホイ</title>
</head>
<body>
    <h1>あっち向いてホイ</h1>
    
    <!-- ユーザーが選んだ方向とコンピュータの選んだ方向を表示 -->
    <p>あなたが選んだ方向: <%= userDirection %></p>
    <p>コンピュータが選んだ方向: <%= cpuDirection %></p>

    <!-- 勝敗の表示 -->
    <p>結果: <%= result %></p>

    <!-- 現在のスコアを表示 -->
    <p>現在 <%= total %> 試合中 <%= win %> 勝しています</p>

    <hr>

    <!-- ユーザーが次の方向を選ぶフォーム -->
    <form action="/janken2" method="get">
        <label for="direction">次はどちらを出しますか？</label><br>

        <input type="radio" name="direction" value="上" required>
        <label for="direction">上</label><br>

        <input type="radio" name="direction" value="右">
        <label for="direction">右</label><br>

        <input type="radio" name="direction" value="下">
        <label for="direction">下</label><br>

        <input type="radio" name="direction" value="左">
        <label for="direction">左</label><br>

        <!-- 勝ち数と試合数を隠しフィールドに渡す -->
        <input type="hidden" name="win" value="<%= win %>">
        <input type="hidden" name="total" value="<%= total %>">

        <input type="submit" value="あっち向いてホイ！">
    </form>   
</body>
</html>
